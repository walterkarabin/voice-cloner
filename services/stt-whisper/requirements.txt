grpcio==1.48.2
grpcio-tools==1.48.2
protobuf>=3.19.0,<3.20
numpy>=1.24.0

# Whisper implementations
# Option 1: faster-whisper (recommended for production, uses CTranslate2)
faster-whisper>=0.10.0

# Option 2: openai-whisper (original implementation, used as fallback)
# NOTE: Requires internet access to download models from https://openaipublic.azureedge.net
openai-whisper>=20231117

# For audio file support in client
soundfile>=0.12.0
